<div>
  <div>
    <div>
      <h4>Calendar</h4>
      <div class="centered">
        <h4>Interested in haring a trail or being bag car*? Email the Boston H3 Hare Raisers at: <a href="mailto:bostonh3hareraiser@gmail.com">bostonh3hareraiser@gmail.com</a></h4>
        <p>*Without bag car, there is no beer! Return the favor and volunteer!</p>
      </div>

			<script type='text/javascript'>
				$(document).ready(function() {
					var calApiKey = 'AIzaSyC93cQGDRfpguqdsNMIEEYRKXeIZzcgr28';
					var calId = 'bostonhash@gmail.com';
				    $('#calendar').fullCalendar({
				        events: {
				        	googleCalendarApiKey: calApiKey,
            				googleCalendarId: calId
        				},
						defaultView: 'month',
					    eventClick: function(calEvent, jsEvent, view) {
					      if (calEvent.url) {
					            window.open(calEvent.url);
					            return false;
					        }
					    }
				    });

				    $('#mobile-calendar').fullCalendar({
				        events: {
				        	googleCalendarApiKey: calApiKey,
            				googleCalendarId: calId
        				},
						defaultView: 'basicWeek',
					    eventClick: function(calEvent, jsEvent, view) {
					      if (calEvent.url) {
					            window.open(calEvent.url);
					            return false;
					        }
					    }
				    });
				});
			</script>

			<div id="calendar" class="hidden-xs"></div>

			<div id="mobile-calendar" class="visible-xs"></div>
    </div>
  </div>
</div>
