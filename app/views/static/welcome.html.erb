<% content_for :head do %>
    <script src="https://maps.googleapis.com/maps/api/js?v=3.exp&sensor=false"></script>
	<script>
	var map;
	var geocoder;
	
	function initialize() {
	  geocoder = new google.maps.Geocoder();
	  var mapOptions = {
	    zoom: 15,
	    center: new google.maps.LatLng(42.3581, -71.0636), //default boston lat & long
	    mapTypeId: google.maps.MapTypeId.ROADMAP
	  };
	  map = new google.maps.Map(document.getElementById('map-canvas'), mapOptions);
	
	  <% if @next_hash.where != "Boston, MA" then  %>
		codeAddress('<%=@next_hash.where%>');
	  <% end %>
	}
	
	function codeAddress(postal) {
	    geocoder.geocode( { 'address': postal + ", United States"}, function(results, status) {
	      if (status == google.maps.GeocoderStatus.OK) {
	        map.setCenter(results[0].geometry.location);
	        var markerE = new google.maps.Marker({
	            map: map,
	            position: results[0].geometry.location,
	        });
	      } else {
	        alert("Geocode was not successful for the following reason: " + status);
	      }
	    });
	}
	
	google.maps.event.addDomListener(window, 'load', initialize);
 	</script>
<% end %>
<% content_for :splash do %>
	<div class="splash">
    <div class="pure-g-r">
        <div class="pure-u-1-3">
            <div class="l-box splash-image">
                <img src="http://placehold.it/500x350"
                     alt="Placeholder image for example.">
            </div>
        </div>

        <div class="pure-u-2-3">
            <div class="l-box splash-text">
                <h1 class="splash-head">
                    We're the Boston Hash House Harriers
                </h1>

                <h2 class="splash-subhead">
                   Some sort of welcome message goes here.  When we're pimping marathon or some other even that can take over this space.  This box will be editable content.  Some kind of default BH3 logo should go left when there is no event on.
                </h2>

                <!-- <p>
                    <a href="#" class="pure-button primary-button">Get Started</a>
                </p> -->
            </div>
        </div>
    </div>
</div>
<% end %>
        <div class="pure-u-2-5">
            <div class="l-box">
                <h4 class="content-subhead">Join us on our next hash!</h4>
				<h3><%= raw @next_hash.title%></h3>
                <h4>When:  <%=@next_hash.when.to_formatted_s(:long)%> (HST) </h4>
				<h4>Where: <%= raw @next_hash.where%></h4>
				<h4>Details:</h4><pre><%=@next_hash.description.strip.html_safe%></pre>
            </div>
        </div>

        <div class="pure-u-3-5">
            <div class="l-box">
				<div id="map-canvas" style="height:400px; width:100%"></div>
            </div>
        </div>
